@model  List<RecipeSite.Models.RecipeListViewModel>

@{
    ViewBag.Title = "Home Page";
}
@*<form method="get">
    <input type="search" name="searchTerm"/>
    <input type="submit" value="Search By Name" />

</form>*@

@using (Ajax.BeginForm(
                    new AjaxOptions
                    {
                        HttpMethod = "get",
                        InsertionMode = InsertionMode.Replace,
                        UpdateTargetId = "recipeList"
                    }))
{
    <input type="search" name="searchTerm" />
    <input type="submit"  value="Search By Name " />
}

<h4>another version of the form</h4>
<form action="@Url.Action("Index")" method="get" data-rs-ajax="true" data-rs-target="#recipeList">
    <input type="search" name="searchTerm" />
    <input type="submit" value="Search By Name " /> 
</form>


@Html.Partial("_Recipes", Model);